/**
 * Matches EXPANDED_WIDTH in the workflow sidebar, so the two rails are the
 * same size. Kept as a local constant rather than imported, to avoid pulling
 * that component into this bundle.
 */
export const RAIL_WIDTH = 200;
