---
phase: 44
plan: 12
type: execute
wave: 7
depends_on: [44-01, 44-02, 44-03, 44-04, 44-05, 44-06, 44-07, 44-08, 44-09, 44-10, 44-11]
files_modified:
  - .planning/phases/44-unified-tabbed-hub-protocols-workflows-marketplace/44-UAT.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [TEST-01, TEST-03]
must_haves:
  truths:
    - "pnpm check, pnpm type-check, node scripts/token-audit.js all exit 0 with zero NEW errors."
    - "pnpm test:e2e --grep 'tabbed-hub|marketplace' all green (or known-issue documented)."
    - "Manual UAT at 1280×800 viewport: load /hub (Protocols active), click each tab, exercise the sort dropdown across Popular/Newest, verify Phase-43 UAT flows still pass inside the Workflows tab."
    - "pnpm discover /hub --auth --highlight, /hub?tab=protocols, /hub?tab=workflows, /hub?tab=marketplace probes captured under tests/e2e/playwright/.probes/."
    - "REQUIREMENTS.md HUBV2-01 wording is updated to reflect the CONTEXT.md decision (default tab = Protocols, not Workflows)."
  artifacts:
    - path: ".planning/phases/44-unified-tabbed-hub-protocols-workflows-marketplace/44-UAT.md"
      provides: "UAT script + checked-off result + screenshot/probe references"
      contains: "UAT"
    - path: ".planning/REQUIREMENTS.md"
      provides: "HUBV2-01 wording corrected to Protocols default"
      contains: "Default active tab is Protocols"
  key_links: []
---

<objective>
Local UAT gate — the binding pre-PR check. Runs the automated suite (`pnpm check` + `pnpm type-check` + `node scripts/token-audit.js` + `pnpm test:e2e --grep "tabbed-hub|marketplace"`), captures `pnpm discover` probes for each tab, exercises every Phase-43 success-criterion path inside the new Workflows tab, and updates REQUIREMENTS.md HUBV2-01 to reflect the locked CONTEXT.md decision (default = Protocols).

Wave-3 task. Depends on every other plan in the phase being merged. Has a checkpoint because the UAT outcome is the human sign-off gate.

Purpose: TEST-01 (local UAT before PR), TEST-03 (lint + type-check + token-audit + e2e all green).
Output: 44-UAT.md committed with PASS verdict; REQUIREMENTS.md HUBV2-01 wording corrected.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/44-unified-tabbed-hub-protocols-workflows-marketplace/44-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run automated gates and write 44-UAT.md</name>
  <files>.planning/phases/44-unified-tabbed-hub-protocols-workflows-marketplace/44-UAT.md</files>
  <read_first>CLAUDE.md (Code Quality section), .planning/phases/44-unified-tabbed-hub-protocols-workflows-marketplace/44-UI-SPEC.md (lines 770-778 — UAT script)</read_first>
  <action>
Run each command and capture the result. The output of `pnpm check` and `pnpm type-check` is auto-saved to `.claude/lint-output.txt` and `.claude/typecheck-output.txt` per the `.claude/settings.json` hooks (CLAUDE.md "Lint Output Caching" section). DO NOT re-run commands to "check progress" — read the cached files instead.

```bash
pnpm check                 # → .claude/lint-output.txt
pnpm type-check            # → .claude/typecheck-output.txt
node scripts/token-audit.js
pnpm test:e2e --grep "tabbed-hub|marketplace"
```

Then run discovery for each tab to capture screenshots and element maps:

```bash
pnpm discover /hub --auth --highlight
pnpm discover /hub?tab=protocols --auth
pnpm discover /hub?tab=workflows --auth
pnpm discover /hub?tab=marketplace --auth --highlight
```

Probes write to `tests/e2e/playwright/.probes/<label>-<timestamp>/`. Reference each probe path in 44-UAT.md.

Re-run the Phase-43 UAT script inside the Workflows tab (HUBV2-06 contract — no Phase-43 regression):

```bash
# Phase-43 UAT lives at .planning/phases/43-hub-ux-overhaul/43-UAT.md.
# Walk each numbered case in that file with the Workflows tab active.
cat .planning/phases/43-hub-ux-overhaul/43-UAT.md
```

Manual cases at 1280×800 viewport (record PASS / FAIL inline in 44-UAT.md):

1. **Default tab is Protocols.** Load `/hub` with no `?tab=`. Confirm Protocols pill is highlighted.
2. **Tab pill click switches content INSTANTLY.** Click Workflows → content swaps; sidebar Sort+Tags appears. No skeleton flicker on the shell.
3. **Tab pill click updates URL via router.replace.** Open DevTools → click Workflows → URL becomes `/hub?tab=workflows`. Browser-back returns to the previous route (NOT to a chain of `?tab=` history entries).
4. **Marketplace tab loads and shows the leaderboard.** Switch to Marketplace. Verify the table header row shows `Rank / Name / Tags / Calls / Price / Chain`. With seeded data, at least one row visible.
5. **Sort dropdown switches between Popular and Newest.** Click the Sort dropdown. Verify two options visible: Popular (default selected, with Check icon), Newest. Click Newest → URL becomes `?sort=newest`, table re-orders by listedAt desc.
6. **Earnings sort is NOT in the dropdown.** Open the Sort dropdown again. Verify there is no "Earnings" option (MARKET-02 deferred).
7. **Privacy assertion via View Source.** On Marketplace tab, View Page Source. Search for `creatorWalletAddress`, `userId`, `organizationId`, `payerAddress`, `amountUsdc`. All zero matches.
8. **No "Listed in marketplace" badge.** On Workflows tab, switch to List view. Inspect rows. Confirm no row carries a "Listed in marketplace" badge or any cross-tab decoration.
9. **Phase-43 UAT inside the Workflows tab.** Walk each numbered case from 43-UAT.md. All must still pass.
10. **Per-tab metadata.** View page source on `/hub?tab=marketplace` → `<title>` reads `Marketplace — Hub | KeeperHub`. Same for `?tab=protocols` → `Protocols — Hub | KeeperHub` and `?tab=workflows` → `Workflows — Hub | KeeperHub`.
11. **Tab-strip search input renders.** Confirm the search input is visible in the tab-strip band, with placeholder text matching the active tab.

Write 44-UAT.md with this structure:

```markdown
# Phase 44 UAT

**Status:** PASS / FAIL
**Date:** <YYYY-MM-DD>
**Tester:** <name>
**Viewport:** 1280×800

## Automated gates

- [ ] pnpm check — green / FAIL: <details>
- [ ] pnpm type-check — green / FAIL: <details>
- [ ] node scripts/token-audit.js — green / FAIL: <details>
- [ ] pnpm test:e2e --grep "tabbed-hub|marketplace" — green / FAIL: <details>

## Manual cases (1280×800)

1. [ ] Default tab is Protocols — PASS / FAIL
2. [ ] Tab pill click switches content INSTANTLY — PASS / FAIL
... (cases 3–11)

## Phase-43 regression sweep (HUBV2-06)

- [ ] All 15 Phase-43 UAT cases re-pass inside the Workflows tab — PASS / FAIL

## Probes

- /hub: tests/e2e/playwright/.probes/hub-default-<timestamp>/
- /hub?tab=protocols: tests/e2e/playwright/.probes/hub-protocols-<timestamp>/
- /hub?tab=workflows: tests/e2e/playwright/.probes/hub-workflows-<timestamp>/
- /hub?tab=marketplace: tests/e2e/playwright/.probes/hub-marketplace-<timestamp>/

## Verdict

PASS / FAIL — explanation
```
  </action>
  <verify>
    <automated>test -f .planning/phases/44-unified-tabbed-hub-protocols-workflows-marketplace/44-UAT.md && grep -ci "verdict" .planning/phases/44-unified-tabbed-hub-protocols-workflows-marketplace/44-UAT.md</automated>
  </verify>
  <acceptance_criteria>
- 44-UAT.md exists with all 11 manual cases listed.
- All four automated gates marked green.
- Phase-43 regression sweep documented.
- Probe directories referenced.
- Verdict line filled in.
  </acceptance_criteria>
  <done>44-UAT.md filled with results.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update REQUIREMENTS.md HUBV2-01 wording (default tab Protocols)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>.planning/REQUIREMENTS.md (line 80 — HUBV2-01 entry), .planning/phases/44-unified-tabbed-hub-protocols-workflows-marketplace/44-CONTEXT.md (Tab Mechanics section, "Default tab when ?tab= is absent: PROTOCOLS")</read_first>
  <action>
Edit `.planning/REQUIREMENTS.md` line 80 (HUBV2-01). The current wording already includes the parenthetical decision footnote ("Default active tab is **Protocols** — first in visual order (overrides the original 'Workflows' default per Phase-44 CONTEXT.md smart-discuss decision, 2026-05-01; user accept).") — confirm this is intact and re-render the requirement so the **Default active tab is Protocols** clause is unambiguous.

If the line already correctly states "Default active tab is **Protocols**" (it does as of the planner's read), make NO edit and document in 44-12-SUMMARY.md that the wording was already correct — no change required.

If the line still reads "Default active tab is Workflows" (regression), correct it to "Default active tab is Protocols" with the same parenthetical footnote.
  </action>
  <verify>
    <automated>grep -c "Default active tab is \*\*Protocols\*\*" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
- `grep -c "Default active tab is \*\*Protocols\*\*" .planning/REQUIREMENTS.md` returns `1`.
- `grep -c "Default active tab is Workflows" .planning/REQUIREMENTS.md` returns `0`.
  </acceptance_criteria>
  <done>HUBV2-01 wording matches the locked CONTEXT.md decision.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: User signs off on UAT before PR</name>
  <what-built>Tasks 1+2 ran the automated gates, captured probes, walked the manual cases, and confirmed REQUIREMENTS.md HUBV2-01 wording.</what-built>
  <how-to-verify>
1. Open `.planning/phases/44-unified-tabbed-hub-protocols-workflows-marketplace/44-UAT.md`.
2. Confirm all four automated gates are green.
3. Walk each of the 11 manual cases at 1280×800 in your browser; mark PASS / FAIL inline.
4. Re-run any Phase-43 UAT path you want to spot-check inside the Workflows tab (HUBV2-06 contract).
5. Inspect probe screenshots under `tests/e2e/playwright/.probes/` for visual regression vs Phase-43 baseline.
6. Type `approved` to proceed to PR creation; type any free-text feedback to trigger gap-closure planning instead.
  </how-to-verify>
  <resume-signal>Type `approved` or list specific gaps.</resume-signal>
</task>

</tasks>

<verification>
- TEST-01 (local UAT done before PR): 44-UAT.md exists with PASS verdict.
- TEST-03 (lint + type-check + token-audit green): all four gates green.
- HUBV2-06 (Phase-43 contracts preserved): regression sweep documented.
- REQUIREMENTS.md HUBV2-01 wording correct.
</verification>

<success_criteria>
- 44-UAT.md committed with PASS verdict.
- Automated gates: `pnpm check` + `pnpm type-check` + `node scripts/token-audit.js` + `pnpm test:e2e --grep "tabbed-hub|marketplace"` all green.
- Probes captured for all four URLs.
- Phase-43 regression sweep PASS.
- REQUIREMENTS.md HUBV2-01 wording reflects the locked CONTEXT.md decision.
</success_criteria>

<output>
After completion, create `.planning/phases/44-unified-tabbed-hub-protocols-workflows-marketplace/44-12-SUMMARY.md` summarizing the UAT verdict + any deferred items.
</output>
